<Page x:Class="ekz.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid>
        <Border Width="320" Height="308" 
                Background="#F0F0F0" CornerRadius="10"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Margin="20,20,20,5">
                <TextBlock Text="Авторизация" 
                           FontSize="22" FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>

                <TextBlock Text="Логин:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLogin" 
                         Height="30" 
                         FontSize="14"
                         Margin="0,0,0,15"/>

                <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" 
                             Height="30" 
                             FontSize="14"
                             Margin="0,0,0,20"/>

                <Button x:Name="btnLogin" 
                        Content="Войти" 
                        Height="35" 
                        FontSize="14"
                        Click="BtnLogin_Click"/>

                <Button x:Name="btnTestDB" Content="Тест БД" 
        Height="28" Margin="0,10,0,0"
        Click="BtnTestDB_Click"
        ToolTip="Проверить подключение к базе данных"/>

                <TextBlock x:Name="lblError" 
                           Foreground="Red" 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0" 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>